# wercker version for box creation
box:
  id: amarburg/lsdslam-dev-host:latest

build:
  steps:
    - script:
      name: Build
      code: |
          ./fips set config g2o-linux-make-unittest
          ./fips build
